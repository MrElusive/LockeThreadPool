package edu.utexas.locke;

// Vince
// This doesn't actually need any more work. However, we do need to create some real world
// problems using LockeThread.
public interface LockeThread {
	public void run(ExecutionContext context);
}
